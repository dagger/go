type Example {
  """
  Go container with a dev engine service and its CLI.
  """
  pub generateEnv: Container! { engineE2e.generateEnv }

  """
  Run this project's Go generator in its selected container.
  """
  pub generate(ws: Workspace!): Changeset! @generate {
    go().module(ws, ".").generate(ws)
  }
}
